# Input Langs

The ccl compiler supports parsing **other** data-types and languages than its own language (ccl); e.g. it can parse openapi json into ccl source code, and then that ccl source code can be used to generate code in other languages.

