# The CCL project

The project root must contain `./agent_docs/AGENTS.md`, refer to that for detailed instructions and project rules.


After you are done with editing, make sure to run `./scripts/EnsureGoContent.ps1` and fix its reported issues/violations.

